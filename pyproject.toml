[project]
name = "mlir-encoder-multiversioner"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
bench = [
    "numpy>=2.5.0",
]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.15.20",
]
